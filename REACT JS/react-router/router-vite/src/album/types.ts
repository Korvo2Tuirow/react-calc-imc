export type Props = {
    albumId: number,
    id: number,
    title: string,
    url: string,
    thumbnailUrl: string,
};

export type Albums = {
    userId: number;
    id: number;
    title: string;
  }