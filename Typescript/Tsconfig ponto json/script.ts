//no terminal digitar tsc --init

const nome: string = "korvo";

console.log(nome);

let num:string = 80; // comando tsc mostra o erro mas gera o arquivo .js

function somar(n1:number, n2:number) {
return n1 + n2;
}
somar(10,20,30);